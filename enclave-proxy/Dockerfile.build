FROM rust:1.75-alpine AS builder
RUN apk add --no-cache musl-dev
WORKDIR /app
COPY Cargo.toml Cargo.lock* ./
COPY src/ src/
RUN cargo build --release
RUN strip /app/target/release/enclave-proxy

FROM scratch
COPY --from=builder /app/target/release/enclave-proxy /enclave-proxy
